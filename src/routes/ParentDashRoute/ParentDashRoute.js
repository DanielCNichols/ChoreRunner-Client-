import React, {Component} from 'react';

/* Ideally, we would get an object that contains 

[
  {
  household_id: num,
  members: [
    {
    member_id: num;
    tasks: [{}}
    exp: num,
    level: num;
    }
  ]
  }
]
*/


//Using the getHouseholds() route.
export default Class ParentDashRoute {
  let state = {
   
 }
}